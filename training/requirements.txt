# Darshana Fine-Tuning Dependencies
# ===================================
# Install: pip install -r training/requirements.txt

# Core ML
torch>=2.1.0
transformers>=4.40.0
peft>=0.10.0
datasets>=2.18.0
accelerate>=0.28.0
bitsandbytes>=0.43.0        # 4-bit quantization (Linux/macOS with CUDA)
trl>=0.8.0                  # RLHF / reward model training

# Inference server
flask>=3.0.0

# Evaluation
scikit-learn>=1.4.0
rouge-score>=0.1.2

# Config
pyyaml>=6.0

# Progress / logging
tqdm>=4.66.0
tensorboard>=2.16.0
